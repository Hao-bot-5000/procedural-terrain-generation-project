using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerVision : MonoBehaviour {
    // Start is called before the first frame update
    void Start() {
        
    }

    // Update is called once per frame
    void Update() {
        
    }

    // private void UpdatePlayerVision() {
    //     if (currentWaterMovementData == null) return;

    //     float waterLevelAtPosition = currentWaterMovementData.GetWaveHeightAtPosition(transform.position);
    //     if (transform.position.y < waterLevelAtPosition) {
    //         RenderSettings.fog = true;
    //         RenderSettings.fogColor = new Color(0f, 0.4f, 0.7f, 0.6f);
    //         RenderSettings.fogDensity = 0.04f; 
    //     }
    //     else {
    //         RenderSettings.fog = true;
    //         RenderSettings.fogColor = new Color(0f, 0.4f, 0.7f, 0.6f);
    //         RenderSettings.fogDensity = 0.04f; 
    //     }
    // }
}
